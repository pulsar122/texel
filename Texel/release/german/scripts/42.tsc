// Berechnet die Fl„che des Kreises mit dem Radius r=argv[2]
// tm 97-11-20

proc main(...)
{
	if (argc<3) exit(1);

	datei = argv[0];
	zelle = argv[1];
	r     = argv[2];
	
	/* Fl„che berechnen */
	wert = pi() * r * r;

	/* Rckgabe des Ergebnisses */
	Texel.ReturnCellValue(datei,zelle,wert);
}
